#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = [" "," "," "," "," "," "," "," "," "]
display_board(array)

puts "Choose a position to play (1-9, where 1 is the top left and 9 is the bottom right)"
move = gets.chop



move(board,input_to_index(position), "X")
display_board(board)
